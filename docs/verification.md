# 動作確認結果（2026-08-22 更新：見積書テンプレート対応後）

## 自動テスト

| 種別 | コマンド | 結果 |
|---|---|---|
| 型チェック | `npm run typecheck` | エラー 0 |
| Lint | `npm run lint` | エラー 0 |
| 単体テスト（価格計算・選択ルール・画像解決） | `npm run test` | 19 / 19 passed（テンプレートの本体価格計・オプション価格計・千円切捨てを再現） |
| E2E（Playwright・ローカルモード・Chromium） | `npm run test:e2e` | 7 / 7 passed（desktop 6 + mobile 1） |
| 本番ビルド | `npm run build` | 成功（警告 1 件：ローカル検証モードの動的パス参照。本番動作に影響なし） |

## 完了条件との対応（E2E で実機確認）

| # | 条件 | 確認方法 | 結果 |
|---|---|---|---|
| 1 | トップページから商品詳細へ移動できる | `flow.spec.ts` 1-2（商品一覧に Wing／BOX／フラットの 3 モデル） | ✅ |
| 2 | 商品詳細から見積シミュレーターを開始できる | `flow.spec.ts` 1-2 | ✅ |
| 3 | お風呂を選択すると画像と金額が変わる | `flow.spec.ts` 3-5：3点ユニット→ユニットバス1216 へ切替で金額が（差額×1.15×1.1）変化。画像は正規の組み合わせ素材が未提供のため「最も近い画像＋未反映：ユニットバス」表示へ切替。実画像の切替はウッドデッキ（外観）・エアコン（室内）で確認 | ✅（画像は要素材） |
| 4 | トイレなど複数オプションを選択できる | `flow.spec.ts` 3-5（トイレ・キッチン・デッキ・エアコン）、プラン切替（ホテルUB）、見積項目クリック→商品選択ポップアップ→変更 | ✅ |
| 5 | 選択できない組み合わせは画面上で説明される | `flow.spec.ts` 3-5：3点ユニット⇔洗面器の競合理由をカード内とトーストに表示、給湯器は依存元があるため外せない旨を表示 | ✅ |
| 6 | ログイン前の選択がログイン後も残る | `flow.spec.ts` 6-10 | ✅ |
| 7 | 保存後にログアウトし、再ログインして再編集できる | `flow.spec.ts` 6-10 | ✅ |
| 8 | 保存した仕様を複製できる | `flow.spec.ts` 6-10 | ✅ |
| 9 | 見積依頼を送信できる | `flow.spec.ts` 6-10（見積番号 `Q202608-0001`、顧客番号 `C000001`、本体諸費用・別途工事セクションを表示） | ✅ |
| 10 | 見積書 PDF を作成できる | `flow.spec.ts` 6-10：`application/pdf`・`%PDF-`。pdftotext で テンプレート構造（本体／オプション／別途工事／小計／値引き等調整額／税抜請負額／消費税／合計、振込先・登録番号）を確認 | ✅ |
| 11 | 管理者が価格と画像を変更できる | `admin.spec.ts`：ユニットバスの価格を +20,000 円 → シミュレーターに（×1.15×1.1 で）反映。`water: bath+washbasin` の画像ルールを追加 → 不足一覧が 1 件減り完全一致表示に | ✅ |
| 12 | 過去の発行済み見積金額は変更されない | `admin.spec.ts` | ✅ |
| 13 | 他人の保存データを閲覧できない | `flow.spec.ts` 6-10（権限エラー／404／401） | ✅ |
| 14 | スマートフォンでも操作できる | `mobile.spec.ts`（Pixel 7）：縦並び、横スクロールなし、タップで金額変化、固定フッター | ✅ |
| 15 | コンソールエラーや主要な型エラーがない | `tsc` エラー 0。E2E で console error / pageerror を収集し 0 件 | ✅ |
| 追加 | BOX・フラットのシミュレーター | `flow.spec.ts`：画像未登録でもプラン・金額が動き、プレビューは「準備中」表示 | ✅ |

## Supabase 実環境での確認について

Docker が使えない環境のため、Supabase（RLS・RPC・Storage）に対する E2E は未実施です。
同じ `DataStore` インターフェースをローカル実装で通しており、Supabase 実装は SQL の関数・ポリシー（`0001`〜`0005`）に処理を委ねています。
本番化の際は README の手順で `supabase db push` → `npm run seed:supabase` → `npm run admin:create` を実行し、`BASE_URL=https://<staging> npm run test:e2e` で同じ E2E を流せます（ローカルモード固有のパスワード再設定テストのみスキップが必要）。
