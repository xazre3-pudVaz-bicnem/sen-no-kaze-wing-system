# 動作確認結果（2026-08-22 更新：先方フィードバック「商品台帳・3つの変更方法・見積書明細」対応後）

## 自動テスト

| 種別 | コマンド | 結果 |
|---|---|---|
| 型チェック | `npm run typecheck` | エラー 0 |
| Lint | `npm run lint` | エラー 0 |
| 単体テスト（価格計算・選択ルール・画像解決） | `npm run test` | 22 / 22 passed（テンプレートの本体価格計・オプション価格計・千円切捨てを再現） |
| E2E（Playwright・ローカルモード・Chromium） | `npm run test:e2e` | 11 / 11 passed（desktop 10 + mobile 1） |
| 本番ビルド | `npm run build` | 成功（警告 1 件：ローカル検証モードの動的パス参照。本番動作に影響なし） |

## 先方フィードバックとの対応

| 先方のご要望 | 実装 | 確認方法 |
|---|---|---|
| 商品台帳を「本体 › 仕様 › 分類 › カテゴリー › 商品」の階層にする | `/admin/ledger`（商品台帳）。本体 3・仕様 3・分類 9・カテゴリー 20・商品 60 | `admin/ledger` を目視。分類は `option_categories.group_code/group_name/group_sort` |
| ①Wing ②BOX ③フラット ④その他（防火／非防火） | 本体 3 モデル＋分類「防火仕様」に 非防火仕様（基本仕様）／防火仕様（防火構造・要代理店相談＝別途見積） | `flow.spec.ts`「防火仕様は代理店相談として別途見積で表示される」 |
| ホテル仕様／住宅仕様／事務所・店舗用で切り替える | 仕様タブ。切り替えで標準構成・平面図・表示商品（`options.spec_codes`）が入れ替わる | `flow.spec.ts` 3-5 末尾 |
| **商品台帳フォルダは増やせるようにして下さい** | 分類フォルダ名・分類コード・表示順を管理画面のカテゴリー編集から入力できる（コード直書きなし） | `/admin/categories` のフォーム |
| Wing を選ぶと 平面図＋立面図4面＋外観パース が出る | 左カラムに平面図・立面図4面・外観パース | `flow.spec.ts` 3-5 冒頭 |
| 標準装備一覧も一緒に表示 | 右カラム「標準設備及び仕上げ表」（カテゴリーごとに現在の商品を画像付きで表示） | 同上 |
| その下に Excel のような見積書 | 「御見積書」セクション（1 本体価格／2 オプション価格＋2-1 明細／3 別途工事／4 運送費／合計） | 同上 |
| **①標準装備一覧をクリック → ポップアップ** | 設備表のタイルをクリックで商品選択ダイアログ | `flow.spec.ts` 3-5「変更方法①」 |
| **②平面図の箇所（UB 等）をクリック → ポップアップ／立面図の面をクリック → 外装選択** | 平面図のホットスポット（UB・トイレ・洗面・エアコン・家具・床・建具）、立面図クリックで外壁選択 | `flow.spec.ts` 3-5「変更方法②」 |
| **③見積書の項目をクリック → ポップアップ** | 2-1 明細の各行がボタン | `flow.spec.ts` 3-5「変更方法③」 |
| 見積書は 1本体／2オプション（明細付き）／3別途工事／4運送費／合計 | 画面・マイページ・管理画面・PDF すべて同じ 4 区分 | `flow.spec.ts` 6-10（`quote-table`） |
| 「運搬、設置費など…現地の代理店、工務店にお問合せ下さい」 | 見積書の下に注記 | `flow.spec.ts` 3-5 |
| 「上をクリックすると代理店紹介の画面で、問合せメール」 | 注記の下のリンク →`/dealers`（別途工事の内訳・拠点一覧・問い合わせフォーム） | `flow.spec.ts`「代理店紹介ページから問い合わせできる」 |

## 完了条件との対応（E2E で実機確認）

| # | 条件 | 確認方法 | 結果 |
|---|---|---|---|
| 1 | トップページから商品詳細へ移動できる | `flow.spec.ts` 1-2（商品一覧に Wing／BOX／フラットの 3 モデル） | ✅ |
| 2 | 商品詳細から見積シミュレーターを開始できる | `flow.spec.ts` 1-2 | ✅ |
| 3 | お風呂を選択すると画像と金額が変わる | `flow.spec.ts` 3-5：ユニットバス 1216 → 3点ユニットへ切替で金額が（差額×1.15×1.1）変化し、水まわり画像が 3点ユニットの写真に**完全一致**で切り替わる | ✅ |
| 4 | トイレなど複数オプションを選択できる | `flow.spec.ts` 3-5（洗面・水栓・給湯・エアコン・デッキ）。3 つの変更方法すべてを操作 | ✅ |
| 5 | 選択できない組み合わせは画面上で説明される | `flow.spec.ts` 3-5：3点ユニット⇔洗面器の競合理由をポップアップ内に表示、給湯器は依存元があるため外せない旨をトーストに表示 | ✅ |
| 6 | ログイン前の選択がログイン後も残る | `flow.spec.ts` 6-10 | ✅ |
| 7 | 保存後にログアウトし、再ログインして再編集できる | `flow.spec.ts` 6-10 | ✅ |
| 8 | 保存した仕様を複製できる | `flow.spec.ts` 6-10 | ✅ |
| 9 | 見積依頼を送信できる | `flow.spec.ts` 6-10（見積番号 `Q202608-0001`、顧客番号 `C000001`、4 区分と代理店注記を表示） | ✅ |
| 10 | 見積書 PDF を作成できる | `flow.spec.ts` 6-10：`application/pdf`・`%PDF-`。本体／オプション＋2-1 明細／別途工事／運送費／小計／値引き等調整額／税抜請負額／消費税／合計、振込先・登録番号 | ✅ |
| 11 | 管理者が価格と画像を変更できる | `admin.spec.ts`：ユニットバスの価格を +20,000 円 → シミュレーターに（×1.15×1.1 で）反映。`water: shower+washbasin` の画像ルールを追加 → 不足一覧が 1 件減る | ✅ |
| 12 | 過去の発行済み見積金額は変更されない | `admin.spec.ts` | ✅ |
| 13 | 他人の保存データを閲覧できない | `flow.spec.ts` 6-10（権限エラー／404／401） | ✅ |
| 14 | スマートフォンでも操作できる | `mobile.spec.ts`（Pixel 7）：平面図 → 設備表 → 御見積書の縦並び、横スクロールなし、タップで金額変化、固定フッター | ✅ |
| 15 | コンソールエラーや主要な型エラーがない | `tsc` エラー 0。E2E で console error / pageerror を収集し 0 件 | ✅ |
| 追加 | BOX・フラットのシミュレーター | `flow.spec.ts`：仕様タブ・標準構成・金額が動く | ✅ |

## 今回みつけて直した不具合

| 症状 | 原因 | 対応 |
|---|---|---|
| 洗面器と混合水栓を同時に外すと、洗面器だけ残った | ポップアップの適用が台帳順で、依存されている「洗面器」を先に外そうとして拒否されていた | 解除を先に、依存元から順に処理するよう `applyPicker` を修正（`components/simulator/simulator-app.tsx`） |
| 標準構成なのに外観・平面図が「最も近い画像」表示だった | 外壁ガルバリウムにプレビューキーを付けた一方、画像ルール側のキーを更新していなかった | 外観ルールのキーを `exterior_galva` 込みに修正（`lib/seed/catalog.ts`） |
| `/dealers` に h1 が無かった | `RuleHeading` が常に h2 | 見出しレベルを指定できるようにし、`/dealers` は h1 に |
| 平面図クリック領域のテスト ID が UUID だった | カテゴリー ID をそのまま出力 | カテゴリーコードに変更（`hotspot-ub` など） |
| マイグレーション未適用の DB にアプリだけ配信すると全ページ 500 | `preview_hotspots` 不在をエラーとして扱っていた | `lib/data/schema-compat.ts` で旧スキーマを既定値に寄せる |

## Supabase 実環境での状態（2026-08-22 時点）

| 項目 | 状態 |
|---|---|
| migrations 0001〜0007 | 適用済み（`contact_messages` を含む） |
| **migrations 0008（商品台帳の3階層化）** | **未適用。デプロイ前に適用が必要**（手順は README 参照） |
| 初期データ | 旧台帳（options 39 件・preview_image_rules 14 件）。0008 適用後に `npm run seed:supabase -- --prune` で新台帳（60 件・18 件・ホットスポット 13 件）へ入れ替え |
| Storage | `product-images`（公開）／`quote-documents`（非公開） |

フル E2E（保存・見積・PDF・権限）は本番 DB にテストデータを残さないため未実施です。
同じ `DataStore` インターフェースをローカル実装で通しており、Supabase 実装は SQL の関数・ポリシーに処理を委ねています。
`BASE_URL=https://<staging> npm run test:e2e` で同じ E2E を流せます（ローカルモード固有のパスワード再設定テストのみスキップが必要）。
